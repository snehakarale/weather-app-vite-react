
import Weather from './components/Weather';


const App: React.FC = () => {

  return (
    <div className='app'>
    
      <Weather />
     
    
     
    </div>
  );
};

export default App;

// --- IGNORE ---
// App is a React functional component that returns JSX to display UI.
// It uses className for styling and is exported so it can be used in other files.
